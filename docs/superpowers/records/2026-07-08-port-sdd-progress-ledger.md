# SDD progress ledger — WordPress→Hugo port (plan 2026-07-07)
# Branch hugo-port. Format: Task N: complete (commits base..head, review clean)
Task 1: complete (commit 23a4f1e..9e249ef, review clean after pyc-cleanup fix; minor note: haiku implementer self-report overclaimed cleanliness — watch for that pattern)
Task 2: complete (no commits — gitignored baseline artifacts in verification/; reviewer independently re-derived all hashes/counts/renders, Approved; surfaced the typed.js subtitle correction)
Task 3: complete (commit 7842063..d95ed79, hugo.toml byte-verified + TOML-parsed, review clean)
Task 4: complete (commit d95ed79..e6c8c92, carve byte-identical cmp exit 0, GATE-DOM empty, GATE-404=baseline; DONE_WITH_CONCERNS both validated by controller: (1) isPlainText necessary+correct [counterfactual: byte-135 divergence without it], (2) Firefox-152 screenshot regression real [reference capture = blank spinner]).
Controller side-fix: 5a9ef40 hardened capture.sh (preloader-hide + freeze-animations + abs path); re-baselined shots-ref/ref2/noise-floor. GATE-PIX now: home/contacts-768 tight, resume/mobile scroll-jitter (judge visually). Plan updated with isPlainText no-autoescape rule (binds Task 7/9) + GATE-PIX reliability note.
Task 4 review: Approved (structural reviewer). Minors carried forward: (M1) content-sidebar/s_overlay divs open in card-profile.html but close in sidebar-widgets.html — warn Task 7 & Task 9 implementers editing those files; DO NOT add a marker comment (breaks byte-identity), gates catch mismatches. (M2) section-about.html keys off theme id card-home-e (About lives in Home tab) — benign.
Task 5: complete (commit 97797e1..d99be4d, title portifolio->portfolio; controller re-ran GATE-DOM independently = exactly one hunk line 11, built title entity intact, tree clean; trivial diff, controller-verified in lieu of separate reviewer).
Task 6: complete (commit d99be4d..275accb, amended once by controller). Controller re-ran GATE-DOM: all 24 changed lines map to allow-list #1(title,2)/#5(search action,2)/#7(metadata removals,14)/#8(localhost configs,6); GATE-404 shrank 3->{/search/} (forward ref to Task 7); no Hugo generator meta leaked (disableHugoGeneratorInject added to hugo.toml, validated); httplocalhost 5->0.
  Controller decisions on implementer's flagged questions: (a) disableHugoGeneratorInject = correct+necessary (Hugo backfills generator on home page). (b) Removed feed=comments-rss2 comments-feed link too (dead WP metadata, same category as feed=rss2, R3 zero-WP) — implementer correctly flagged rather than guessed. (c) application/json REST alternate link removed with api.w.org (both dead WP REST). All within spec §6 #7's "dead WP head metadata" category — final reviewer: consult this note.
  NOTE for Task 6e: the wpcf7 inline config (now /index.php?rest_route=/) and CF7 JS get removed in 6e; the /index.php ref disappears then.
Task 6d: complete (commit 275accb..e677541, CV href -> /media/Mohamed_Aly_Amin_CV.pdf + download attr; PDF Mohamed-confirmed current July-2026 2pg). Controller verified: incremental DOM = exactly one anchor line (#3), PDF tracked + HTTP 200, tree clean. Human checkpoint CLEARED (Mohamed: "use this one").
Task 6e: complete (commit e677541..a809150, form->Formspree + honeypot + _subject + contact-form.js, CF7 JS removed). Controller verified: both DOM hunks map to #2, honeypot(display:none)/_subject(hidden) invisible, CF7 js gone, contact-form.js resolves 200; read the JS myself — success/error/honeypot/no-JS-fallback logic sound. GATE-404 {/search/}. PIX home 0/0/0; contacts-e-768 flake = scroll-jitter (fields invisible+below fold).
  WATCH-ITEMS for final review/Task 12: (1) LIVE email+JS-UI test still PENDING — do with Mohamed in-browser (first POST also activates his Formspree form). Endpoint confirmed live (GET->405). (2) Dead _wpcf7* hidden fields remain in the form (byte-faithful); Formspree ignores _-prefixed unknown fields so no email clutter expected — confirm during live test, clean up only if they appear.
  Controller side-commit after 6e: corrected plan GATE-PIX note (only home-* reliable; all #anchor widths scroll-flaky).
Task 7: complete (base 14ae769 -> commits 7b53efe + ca9d138). Interrupted mid-run by API error; controller finished + verified. Home incremental diff = only #4/#6/#10; GATE-404 exit 0; 4 new surfaces render correctly (screenshots viewed).
  Controller-found+fixed bugs: (1) NAV HIJACK — theme one-page handler return-false'd ALL .top-menu anchors; added guard in ryan-scripts.js so real-path links navigate. (2) dup data-id BLOG -> 112. (3) DOM-XSS in search.html innerHTML -> rebuilt with DOM APIs (also flagged by commit security review).
  Mohamed checkpoint CLEARED; 404 redesigned to BSOD/hacker-terminal per request. ryan-scripts.js edited (nav guard) -> moves to /vendor in Task 10.
Task 9: complete (commits 8296467 profile [prior window], c8f97ae resume, 9ee6628 contact). GATE-BYTE: home + blog builds byte-identical to pre-Task-9 snapshot. Left literal: profile typed.js roles (whitespace/2nd-copy), contact labels. Working tree clean.
NOTE: task board store was cleared when session backgrounded; rebuilt active board (Tasks 10-13 remain). All completed work is in git (HEAD 9ee6628) + this ledger.
Task 10: complete (commit 399ecc1). R3 MILESTONE — GATE-WP: 0 matches repo source + 0 built output (verified correctly, no pipe-masking). GATE-404 exit 0 (81 refs). GATE-DOM 96 lines all path substitutions (the 1 flagged "added" line = JSON-escaped \/vendor\/elementor\/assets\/, verified correct). Home renders fully styled (fonts/icons/images load from /theme /vendor /media). Nav guard survived at static/theme/assets/js/ryan-scripts.js. git clean.
  Sub-fix by implementer: a JSON-escaped \/wp-content\/ path in tail-scripts.html the plain sed missed — found via fixed-string search, fixed.
Task 11: complete (commit 6aa4b8e). deploy.yml verified: Hugo 0.163.3 extended pinned, no --minify, check_404 link-check gate, Pages deploy on main push + PR build. YAML valid, link-check exit 0 local.
Task 12: complete — PR #6 created, CI green (after Mohamed cleared a GitHub billing lock), verification/summary.md evidence, +2 review fixes (unclosed divs, double-submit guard), +CNAME moved to static/.
Task 13: complete — GO LIVE done. Pages source flipped to GitHub Actions (build_type=workflow), rollback branch wordpress-export-backup@b4d128a pushed, PR #6 merged to main (0c37c29), deploy workflow build+deploy SUCCESS. Live verified on https://mohamedalyamin.com: title fixed, formspree present, 0 wp-content, /theme /media assets 200, /blog/ /search/ /index.xml 200, BSOD 404 serves, custom domain intact, home renders identical. PORT COMPLETE + DEPLOYED.

### WP CLEANUP (branch wp-cleanup, base 6d6704f)
Task 0: complete — baseline captured. Firefox pixel baseline 14 PNGs in verification/cleanup-baseline/. chrome-MCP console+network baseline in verification/cleanup-notes.md. KNOWN pre-existing: favicon.ico 404 (no favicon on site) + JQMIGRATE log — both baseline, not regressions. Verify = hybrid (chrome-MCP console/network + Firefox pixel; qutebrowser CDP can't screenshot).
