# Selected open-source contributions (his go 2026-07-11: small + humble,
# external-validation items only). StakeDex deliberately NOT here — it's
# Sardis's own org, folded into the Sardis experience entry instead.
contributions:
  - name: "waybar-crypto"
    detail: "PR #105 (merged): custom formatting options — global and per-coin format strings, case-sensitive ticker support."
    url: "https://github.com/chadsr/waybar-crypto/pull/105"
  # opencode PR #9208 removed 2026-07-13 (projects deep-check): PR closed
  # UNMERGED — fails the "external-validation only" rule; a rejected PR as
  # portfolio proof is negative signal.
